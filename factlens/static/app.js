const $=s=>document.querySelector(s); let data={relations:[],documents:[],facts:[],diagnostics:[]}, filter='all';
function evidence(f){return `<div class="evidence"><b>${escape(f.evidence.document_name)}</b><small>Page ${f.evidence.page} · ${f.kind}${f.value?` · ${escape(f.value)}`:''}${f.period?` · ${escape(f.period)}`:''}</small><p>“${escape(f.evidence.excerpt)}”</p></div>`}
function escape(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]))}
function render(){const rel=data.relations.filter(r=>filter==='all'||r.type===filter);$('#metrics').innerHTML=[['Documents',data.documents.length],['Candidate facts',data.facts.length],['Relationships',data.relations.length],['Watchlist',data.diagnostics.length]].map(([l,n])=>`<div class="metric"><strong>${n}</strong><span>${l}</span></div>`).join('');$('#relations').innerHTML=rel.length?rel.map(r=>`<article class="relation"><div class="relation-head"><span class="badge ${r.type}">${r.type.toUpperCase()}</span><span class="confidence">${Math.round(r.confidence*100)}% signal</span></div><p class="reason">${escape(r.reason)}</p><div class="evidence-grid">${evidence(r.left)}${evidence(r.right)}</div></article>`).join(''):'<div class="empty">No relationships in this view yet. Upload overlapping documents or choose another filter.</div>';$('#diagnostics').innerHTML=data.diagnostics.length?data.diagnostics.map(d=>`<div class="diagnostic"><b>${escape(d.type.replace('_',' '))}</b><br>${escape(d.message)}</div>`).join(''):'<div class="empty">No extraction failures reported. This does not guarantee every fact was captured.</div>'}
async function refresh(){data=await fetch('/api/knowledge').then(r=>r.json());render()}
$('#upload').onclick=async()=>{let files=$('#files').files;if(!files.length)return $('#status').textContent='Choose at least one PDF.';$('#status').textContent='Extracting facts and comparing evidence…';let form=new FormData;[...files].forEach(f=>form.append('files',f));let r=await fetch('/api/documents',{method:'POST',body:form}), out=await r.json();if(!r.ok){$('#status').textContent=out.error;return}data=out;$('#status').textContent=`Analyzed ${files.length} document(s).`;render()};
document.querySelectorAll('[data-filter]').forEach(b=>b.onclick=()=>{filter=b.dataset.filter;document.querySelectorAll('[data-filter]').forEach(x=>x.classList.toggle('active',x===b));render()});$('#reset').onclick=async()=>{await fetch('/api/reset',{method:'POST'});await refresh();$('#status').textContent='Workspace reset.'};refresh();
